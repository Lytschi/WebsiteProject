$(document).ready(function() {
    $(document).on("click", "#testquery", function() {
        toggleParagraphVisibility();
    });

    function toggleParagraphVisibility() {
        $("#Para").toggle(); // Versteckt oder zeigt den Paragraphen basierend auf seinem aktuellen Zustand
    }
});
